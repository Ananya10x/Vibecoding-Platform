
        // JavaScript for dynamic features (example)
        document.querySelectorAll('.feature-card').forEach(card => {
            card.addEventListener('click', () => {
                alert('Feature details will be displayed here.');
            });
        });
    